# Replays and parser output (per CLAUDE.md)
data/replays/
data/extracted/

# Generated DB — regenerable from seeds via `python -m sc2tc.db.build`
data/sc2tc.db

# Claude API key for the agent's claude backend (never commit secrets)
.anthropic_key

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
build/
dist/
